# БЛОК: БАЙТ-КОМПИЛИРОВАННЫЕ И ОПТИМИЗИРОВАННЫЕ ФАЙЛЫ
# Кэш Python, создаваемый при импорте модулей
__pycache__/
# Скомпилированные байт-кодом файлы Python
*.py[cod]
# Временные файлы, создаваемые при компиляции
*$py.class

# БЛОК: РАСШИРЕНИЯ C
# Скомпилированные библиотеки C
*.so

# БЛОК: ДИСТРИБУЦИЯ И УПАКОВКА
# Директории и файлы сборки пакетов
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
# Метаданные пакетов, устанавливаемых через pip
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# БЛОК: PyInstaller
# Файлы, генерируемые PyInstaller при сборке исполняемого файла
*.manifest
*.spec

# БЛОК: ЛОГИ УСТАНОВКИ ПАКЕТОВ
# Логи pip при установке
pip-log.txt
pip-delete-this-directory.txt

# БЛОК: ОТЧЁТЫ О ТЕСТИРОВАНИИ И ПОКРЫТИИ КОДА
# Директория с HTML-отчётами о покрытии
htmlcov/
# Артефакты tox для автоматизации тестирования
.tox/
.nox/
# Файлы с данными о покрытии кода
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
# Кэш pytest и hypothesis
.hypothesis/
.pytest_cache/
cover/

# БЛОК: ПЕРЕВОДЫ (TRANSLATIONS)
# Скомпилированные файлы переводов
*.mo
*.pot

# БЛОК: DJANGO
# Логи Django-приложений
*.log
# Локальные настройки, специфичные для окружения разработчика
local_settings.py
# Файлы базы данных SQLite
db.sqlite3
db.sqlite3-journal

# БЛОК: FLASK
# Данные экземпляра Flask-приложения
instance/
# Кэш веб-ресурсов Flask
.webassets-cache

# БЛОК: SCRAPY
# Артефакты фреймворка Scrapy
.scrapy

# БЛОК: SPHINX (ДОКУМЕНТАЦИЯ)
# Сборка документации Sphinx
docs/_build/

# БЛОК: PyBuilder
# Артефакты PyBuilder
.pybuilder/
target/

# БЛОК: JUPYTER NOTEBOOK
# Контрольные точки Jupyter
.ipynb_checkpoints

# БЛОК: IPYTHON
# Профиль и настройки IPython по умолчанию
profile_default/
ipython_config.py

# pyenv
#   Для библиотеки или пакета может потребоваться игнорировать эти файлы, поскольку код
#   предназначен для запуска в нескольких окружениях; в противном случае добавьте их в систему контроля версий:
# .python-version

# pipenv
#   Согласно рекомендации pypa/pipenv#598, рекомендуется включать Pipfile.lock в систему контроля версий.
#   Однако в случае совместной работы при наличии платформозависимых зависимостей или зависимостей,
#   не имеющих кроссплатформенной поддержки, pipenv может установить неработающие зависимости
#   или не установить все необходимые.
#Pipfile.lock

# poetry
#   Аналогично Pipfile.lock, обычно рекомендуется включать poetry.lock в систему контроля версий.
#   Это особенно рекомендуется для бинарных пакетов для обеспечения воспроизводимости и реже
#   применяется для библиотек.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Аналогично Pipfile.lock, обычно рекомендуется включать pdm.lock в систему контроля версий.
#pdm.lock
#   pdm хранит конфигурации уровня проекта в .pdm.toml, но рекомендуется не включать его
#   в систему контроля версий.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# БЛОК: ВИРТУАЛЬНЫЕ ОКРУЖЕНИЯ
# Директории с виртуальными окружениями
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/